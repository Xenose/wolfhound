CMAKE_MINIMUM_REQUIRED(VERSION 3.28)

SET(PROJECT_NAME socket_server)

PROJECT(${PROJECT_NAME} LANGUAGES C)
ADD_EXECUTABLE(${PROJECT_NAME} "src/source.c")
TARGET_LINK_LIBRARIES(${PROJECT_NAME} PRIVATE wolfhound)
