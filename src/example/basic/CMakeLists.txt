CMAKE_MINIMUM_REQUIRED(VERSION 3.28)

ADD_SUBDIRECTORY(engine)
PROJECT(editor LANGUAGES C Fortran)

SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -Wall -pg -O0")

INCLUDE_DIRECTORIES("engine/include")

ADD_EXECUTABLE(basic "src/source.c")
TARGET_LINK_LIBRARIES(basic PRIVATE wolfhound)
